stb single-file public-domain (MIT alt) libraries

Source: https://github.com/nothings/stb
Commit: 31c1ad37456438565541f4919958214b6e762fb4
Files:  stb_image.h, stb_image_write.h, stb_image_resize2.h, stb_rect_pack.h, LICENSE

Used by:
  - scripts/octarine-icon-tool  (host-only resize + .ico encoder)
  - src/AssetManager/AtlasBaker (bake-time texture atlas packing —
    stb_image + stb_image_write + stb_rect_pack; only compiled into the
    engine binary so the bake step has them, never invoked at runtime
    outside `-m bake`).

License attribution rides along via the libs/*/LICENSE* path in
cmake/octarine-licenses.cmake.

To bump: re-fetch each header at the new commit, update the SHA above,
and verify the icon byte-for-byte tests in scripts/verify-icons.sh + the
atlas baker's deterministic ordering check.
